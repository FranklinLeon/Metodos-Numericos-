% Autores: Franklin Leon, Jonathan Pacheco, Luis Yanza
%
% Fecha: 14/10/2021
% Descripcion: Se presentan dos ejemplos de la implementación del script que simula el método
% de iterativo de punto fijo, el cual tiene como objetivo, aproximar la raíz de una funcion cualquiera.
% El programa será capaz de mostrar las iteraciones con sus respectivos valores, además de un
% gráfico que muestra los puntos de cada iteración así como el trazado de la función. 

% Problema 1:
% 
% La primera función a probar será: (6 - x) ^ (1 / 3)
% A la cual le daremos unpunto de inicio de 1 y una tolerancia de 0.0002 y un maximo de 12 iteraciones
g = @(x) x.^3+2*x.^2+10*x-20
p0=1
N=12
tol=0.0002
figure(1);
try 
  fplot(g,[-10,10])
  hold on;
  fprintf('Metodo de punto fijo\n')
  fprintf('   i      p     error\n')
  p=pf(g,p0,N,tol);
  
  while i<N
    p=g(p0);
    fprintf('    %4.0f     %4.5f   %4.6f\n',i,p,abs(p-p0))
    plot(p,))
    if abs(p-p0)<tol
      fprint('Se terminaron las iteraciones %4.0f', i);
      break
    else
      p0=p;
      i+=1;
    end
  end    
end
 
% Problema 2:
%   
% La primera función a probar será: cos(x)- x^3
% A la cual le daremos unpunto de inicio de 1 y un porcentaje de error de 0.00001.
%funcion='cos(x)- x^3'
%derivada ='-sin(x)-3*x^2'
%pi=1
%err=0.00001
%figure(2);
%try 
%  newton(funcion,derivada, pi,err);
%end 
